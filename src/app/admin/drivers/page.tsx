import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function DriversPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Drivers</CardTitle>
        <CardDescription>Onboard, view, and manage all drivers in the system.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Drivers management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
